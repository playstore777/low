import GoogleG from "../../assets/images/MediumGoogleGLogo.svg";
import SvgWrapper from "../reusableComponents/svg/SvgWrapper";
import "./Login.css";

const Login = () => {
  return (
    <div className="loginWrapper">
      Should be converted to sign up form, this should be used to get the sign
      up data, such as username etc.
    </div>
  );
};

export default Login;
